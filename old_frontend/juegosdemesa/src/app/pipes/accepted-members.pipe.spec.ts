import { AcceptedMembersPipe } from './accepted-members.pipe';

describe('AcceptedMembersPipe', () => {
  it('create an instance', () => {
    const pipe = new AcceptedMembersPipe();
    expect(pipe).toBeTruthy();
  });
});
